* Start clock server first with parameter: -lp 3779
* Other servers and all clients all synchronize automatically with clock server. If clock server not found they will crash
* other testings same as before